import { ImageButton } from "components/Buttons/Buttons"
import { Grid, PageTemplate } from "components/Containers/Containers"

export const Home = () => {
  return <PageTemplate>
    <Grid>
      <ImageButton imgUrl="" altText="" />
    </Grid>
  </PageTemplate>
}